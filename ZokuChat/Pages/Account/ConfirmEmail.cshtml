@page
@model ConfirmEmailModel
@{
	ViewData["Title"] = "Confirm Email";
}

<partial name="_SiteHeader" />
<div class="content">
	<h3>Confirm Email</h3>
	<p>@Model.Message</p>
@if (Model.IsResendVisible)
{
	<div>
		<button asp-action="post" class="btn btn-primary">Resend</button>
	</div>
}
</div>